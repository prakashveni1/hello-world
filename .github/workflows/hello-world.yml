name: Hello World

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Say Hello
        run: |
          echo "Hello GitHub Actions!"
          echo "This is a basic example."
